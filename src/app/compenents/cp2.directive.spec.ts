import { Cp2Directive } from './cp2.directive';

describe('Cp2Directive', () => {
  it('should create an instance', () => {
    const directive = new Cp2Directive();
    expect(directive).toBeTruthy();
  });
});
