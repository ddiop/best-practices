import { ApendDemoDirective } from './apend-demo.directive';

describe('ApendDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new ApendDemoDirective();
    expect(directive).toBeTruthy();
  });
});
