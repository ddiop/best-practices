import { RemoveChildDirective } from './remove-child.directive';

describe('RemoveChildDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveChildDirective();
    expect(directive).toBeTruthy();
  });
});
