import { SetAndRemoveAttributeDirective } from './set-and-remove-attribute.directive';

describe('SetAndRemoveAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new SetAndRemoveAttributeDirective();
    expect(directive).toBeTruthy();
  });
});
