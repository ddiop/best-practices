import { SetPropertyDirective } from './set-property.directive';

describe('SetPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new SetPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
