import { AppendChildDirective } from './append-child.directive';

describe('AppendChildDirective', () => {
  it('should create an instance', () => {
    const directive = new AppendChildDirective();
    expect(directive).toBeTruthy();
  });
});
