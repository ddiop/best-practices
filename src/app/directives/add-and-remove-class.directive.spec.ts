import { AddAndRemoveClassDirective } from './add-and-remove-class.directive';

describe('AddAndRemoveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AddAndRemoveClassDirective();
    expect(directive).toBeTruthy();
  });
});
