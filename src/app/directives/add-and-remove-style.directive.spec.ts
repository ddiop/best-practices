import { AddAndRemoveStyleDirective } from './add-and-remove-style.directive';

describe('AddAndRemoveStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new AddAndRemoveStyleDirective();
    expect(directive).toBeTruthy();
  });
});
