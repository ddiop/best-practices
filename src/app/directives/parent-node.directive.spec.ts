import { ParentNodeDirective } from './parent-node.directive';

describe('ParentNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new ParentNodeDirective();
    expect(directive).toBeTruthy();
  });
});
