import { InsertBeforeDirective } from './insert-before.directive';

describe('InsertBeforeDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertBeforeDirective();
    expect(directive).toBeTruthy();
  });
});
